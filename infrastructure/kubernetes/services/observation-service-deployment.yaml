apiVersion: apps/v1
kind: Deployment
metadata:
  name: observation-service
  namespace: omyfish
  labels:
    app: observation-service
    app.kubernetes.io/part-of: omyfish
spec:
  replicas: 2
  selector:
    matchLabels:
      app: observation-service
  template:
    metadata:
      labels:
        app: observation-service
        app.kubernetes.io/part-of: omyfish
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: observation-service
          image: omyfish/observation-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: omyfish-config
            - secretRef:
                name: omyfish-secret
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: observation-service
  namespace: omyfish
  labels:
    app: observation-service
spec:
  selector:
    app: observation-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
